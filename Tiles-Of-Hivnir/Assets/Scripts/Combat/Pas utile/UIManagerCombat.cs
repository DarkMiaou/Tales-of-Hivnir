using UnityEngine;
using UnityEngine.UI;
using System.Collections;
using System.Collections.Generic;

/*namespace TalesofHivnir
{


    public class UIManagerCombat : MonoBehaviour
    {
        [SerializeField] private Button toggleViewButton;
        [SerializeField] private GameObject mainGameView;
        [SerializeField] private GameObject blockView;

        private bool isBlockView;

        void Start()
        {
            toggleViewButton.onClick.AddListener(ToggleView);

            // Set initial view state
            isBlockView = false;
            mainGameView.SetActive(true);
            blockView.SetActive(false);
        }

        void ToggleView()
        {
            isBlockView = !isBlockView;

            mainGameView.SetActive(!isBlockView);
            blockView.SetActive(isBlockView);
        }
    }
}*/
